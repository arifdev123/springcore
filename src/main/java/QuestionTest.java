
public class QuestionTest {

	public static void main(String[] args) {
		/*
		 * int count = 0;
		 * 
		 * ROW_LOOP: for(int row = 1; row <= 3; row++) for(int col = 1; col <= 2 ;
		 * col++) { if(row * col % 2 == 0) continue ROW_LOOP; count++; }
		 * 
		 * System.out.println(count); // 2
		 */
		
		
		/*
		 * int m = 9, n = 1, x = 0; while(m > n) { m--; n += 2; x += m + n; }
		 * System.out.println(x); // 36
		 */	
		
		/*
		 * for(int i=0; i<10; ) { i = i++; System.out.println("Hello World!"); // Prints
		 * infinetly as i is always evaluates to zero which keeps the condition i<10
		 * always true. //System.out.println(i++); // System.out.println(i); }
		 */
		
		
		/*
		 * boolean x = true, z = true;
		 * 
		 * int y =20; x = (y != 10) ^ (z=false); System.out.println(x +", "+y+"," +z);
		 */
		 
		 
		 
		 
	}

}
